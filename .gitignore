# Planning / brief / agent-only artifacts (not public docs)
VISION.md
GOAL.md
AGENTS.md
TODO.md

# Private docs: architecture notes, protocol mapping, loom scripts, anything
# not meant for the public README. See CLAUDE.md "Private docs" for the rule.
private-docs/

# Vendored upstream protocol specs (large, mirror of public repos; re-fetch on demand).
# Leading slash anchors to repo root only — must NOT match docs/protocols/.
/protocols/

# Node / TypeScript / monorepo build outputs
node_modules/
dist/
build/
.turbo/
coverage/
*.tsbuildinfo

# Test outputs
.vitest-cache/
verify-report.json

# mkdocs build output + cache
site/
.cache/

# Env files (NEVER commit secrets — Stripe keys, Anthropic keys, etc.)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Temp
/tmp/
*.tmp
VERIFY.md

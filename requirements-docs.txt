# Steelyard docs — Python deps for mkdocs-material.
# Build:   pip install -r requirements-docs.txt && mkdocs serve
# Deploy:  mkdocs gh-deploy --force
mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
pymdown-extensions>=10

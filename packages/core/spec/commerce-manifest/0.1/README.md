# Commerce Manifest Schema 0.1

This directory contains Steelyard-authored JSON Schemas for the public
`/.well-known/commerce.json` document.

Within the `0.x` line, schema changes must be additive. Breaking changes require
a new major schema path and a new `$id`.
